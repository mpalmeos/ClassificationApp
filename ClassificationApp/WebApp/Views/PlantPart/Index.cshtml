@model IEnumerable<Domain.PlantPart>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PlantPartValueLatin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlantPartValueEstonian)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlantPartValueEnglish)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PlantPartValueLatin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PlantPartValueEstonian)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PlantPartValueEnglish)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
